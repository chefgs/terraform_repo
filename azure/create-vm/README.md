# Azure Create VM Sample

This sample provisions an Azure Resource Group and Virtual Network using the `azurerm` Terraform provider.

## Usage

1. Log in to Azure:
   ```bash
   az login
   ```

2. Initialize and apply:
   ```bash
   terraform init
   terraform plan
   terraform apply
   ```

3. Clean up resources:
   ```bash
   terraform destroy
   ```
